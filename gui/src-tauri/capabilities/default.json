{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the window needs and nothing more: choosing files, receiving progress events, and revealing a finished conversion in the file manager.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:allow-reveal-item-in-dir"
  ]
}
