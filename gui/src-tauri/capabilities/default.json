{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "画面が必要とする権限。ファイル選択、進捗イベント、そして変換結果を Finder で表示することだけ。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:allow-reveal-item-in-dir"
  ]
}
